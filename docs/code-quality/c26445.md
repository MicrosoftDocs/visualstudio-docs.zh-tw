---
title: C26445
ms.date: 03/22/2018
ms.topic: reference
f1_keywords:
- C26445
helpviewer_keywords:
- C26445
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 539774aa6fdfe2f4d92fb604db7200b7844c1c32
ms.sourcegitcommit: 535ef05b1e553f0fc66082cd2e0998817eb2a56a
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/07/2019
ms.locfileid: "72015509"
---
# <a name="c26445-no_span_ref"></a>C26445 NO_SPAN_REF
@No__t-0 或 `std::string_view` 的參考可能表示存留期的問題。
C++核心指導方針：[GSL。 view：Views @ no__t-0

此規則的目的是要攔截程式碼中可能會發生的微小存留期問題，這些問題已從標準容器遷移到新的範圍和檢視類型。 這類類型可以視為「緩衝區的參考」。 使用 span 或 view 的參考會建立額外的間接取值層。 這類間接取值通常不是必要的，而且可能會對維護人員造成混淆。 此外，範圍較低而無法複製，而且可由函式呼叫中的值傳回。 很明顯地，這類呼叫結果永遠不會被參考。

## <a name="remarks"></a>備註

- 此規則會偵測到 gsl：： span < >、gsl：： basic_string_span < > 和 std：： basic_string_view < > （包括要具現化的別名）的參考。
- 目前只有在宣告和傳回語句上才會發出警告。 這可能會在未來延伸，以加上旗標函式參數。
- 此規則的執行非常輕量，不會嘗試追蹤實際的存留期。 在某些情況下，使用參考可能仍有意義。 在這種情況下，可以安全地隱藏誤報。

## <a name="example-reference-to-a-temporary"></a>範例：暫存的參考

```cpp
// Old API - uses string reference to avoid data copy.
const std::string& get_working_directory() noexcept;

// New API – after migration to C++17 it uses string view.
std::string_view get_working_directory() noexcept;

// ...
// Client code which places an explicit reference in a declaration with auto specifier.
const auto &wd = get_working_directory(); // C26445 after API update.
```
